# Copy to .env and adjust. SQLite is the zero-setup default.
DATABASE_URL=sqlite:///./dev.db
# Production (Postgres) example:
# DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5432/ttos
API_PREFIX=/v1
